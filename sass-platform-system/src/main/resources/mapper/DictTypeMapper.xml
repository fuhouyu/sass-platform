<?xml version="1.0" encoding="UTF-8"?>
<!--
  Copyright 2024-2025 fuhouyu.
 
  Licensed under the Apache License, Version 2.0 (the "License");
  you may not use this file except in compliance with the License.
  You may obtain a copy of the License at
 
       https://www.apache.org/licenses/LICENSE-2.0
 
  Unless required by applicable law or agreed to in writing, software
  distributed under the License is distributed on an "AS IS" BASIS,
  WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
  See the License for the specific language governing roles and
  limitations under the License.
-->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fuhouyu.sass.platform.system.mapper.DictTypeMapper">
    <resultMap id="BaseResultMap" type="com.fuhouyu.sass.platform.system.entity.DictType" extends="com.fuhouyu.sass.platform.system.mapper.BaseMapper.BaseResultMap">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="dict_name" property="dictName" jdbcType="VARCHAR"/>
        <result column="dict_code" property="dictCode" jdbcType="VARCHAR"/>
        <result column="display_order" property="displayOrder" jdbcType="INTEGER"/>
        <result column="is_enabled" property="isEnabled" jdbcType="BOOLEAN"/>
        <result column="is_allow_modified" property="isAllowModified" jdbcType="BOOLEAN"/>
        <result column="owner_tenant_id" property="ownerTenantId" jdbcType="VARCHAR"/>
    </resultMap>

    <sql id="TableName">
        dict_type
    </sql>

    <sql id="AllColumns" >
        id, 
        dict_name,
        dict_code,  
        display_order,  
        remark, 
        is_enabled, 
        is_allow_modified,
        owner_tenant_id,
        is_deleted, 
        created_at,
        created_by,
        updated_at,
        updated_by
    </sql>

    <sql id="QueryColumns" >
        dt.id, 
        dt.dict_name,
        dt.dict_code,  
        dt.display_order,  
        dt.remark, 
        dt.is_enabled, 
        dt.is_allow_modified,
        dt.owner_tenant_id,
        dt.is_deleted, 
        dt.created_at,
        dt.created_by,
        dt.updated_at,
        dt.updated_by
    </sql>

    <insert id="insert" parameterType="com.fuhouyu.sass.platform.system.entity.DictType">
        INSERT INTO
        <include refid="TableName"/>
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <include refid="AllColumns"/>
        </trim>
        VALUES
        <trim prefix="(" suffix=")" suffixOverrides=",">
            #{id},
            #{dictName},
            #{dictCode},
            #{displayOrder},
            #{remark},
            #{isEnabled},
            #{isAllowModified},
            #{ownerTenantId},
            #{isDeleted},
            #{createdAt},
            #{createdBy},
            #{updatedAt},
            #{updatedBy},
        </trim>
    </insert>

    <insert id="insertBatch" parameterType="java.util.List" >
        INSERT INTO
        <include refid="TableName"/>
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <include refid="AllColumns"/>
        </trim>
        VALUES
        <foreach collection="list" separator="," item="entity">
            <trim prefix="(" suffix=")" suffixOverrides=",">
                #{entity.id},
                #{entity.dictName},
                #{entity.dictCode},
                #{entity.displayOrder},
                #{entity.remark},
                #{entity.isEnabled},
                #{entity.isAllowModified},
                #{entity.ownerTenantId},
                #{entity.isDeleted},
                #{entity.createdAt},
                #{entity.createdBy},
                #{entity.updatedAt},
                #{entity.updatedBy},
            </trim>
        </foreach>
    </insert>

    <update id="update" parameterType="com.fuhouyu.sass.platform.system.entity.DictType">
        UPDATE
        <include refid="TableName"/>
        SET

        <if test="dictName != null and dictName != ''">
            dict_name = #{dictName},
        </if>
        <if test="displayOrder != null ">
            display_order = #{displayOrder},
        </if>
        <if test="isEnabled != null ">
            is_enabled = #{isEnabled},
        </if>
        <if test="remark != null ">
            remark = #{remark},
        </if>
        updated_at = #{updatedAt},
        updated_by = #{updatedBy}
        WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="java.lang.Long">
        DELETE FROM <include refid="TableName"/>
        WHERE id = #{id}
    </delete>

    <delete id="deleteByIds" parameterType="java.util.List">
        DELETE FROM <include refid="TableName"/>
        WHERE id IN
        <foreach collection="list" open="(" close=")" separator="," item="id">
            #{id}
        </foreach>
    </delete>

    <select id="queryById" resultMap="BaseResultMap">
        SELECT <include refid="QueryColumns"/>
        FROM <include refid="TableName"/> dt
        WHERE dt.id = #{id}
    </select>


    <select id="queryByIds" parameterType="java.util.List" resultMap="BaseResultMap">
        SELECT <include refid="QueryColumns"/>
        FROM <include refid="TableName"/> dt
        WHERE dt.id IN
        <foreach collection="list" open="(" close=")" separator="," item="id">
            #{id}
        </foreach>
    </select>


    <select id="queryByDictCode" parameterType="java.lang.String" resultMap="BaseResultMap">
        SELECT <include refid="QueryColumns"/>
        FROM <include refid="TableName"/> dt
        WHERE dt.dict_code = #{dictCode}
    </select>

    <select id="queryList" resultMap="BaseResultMap" >
        SELECT
        <include refid="QueryColumns"/>
                FROM
        <include refid="TableName"/> dt
        <where>
            <if test="pageQuery.dictCode != null and pageQuery.dictCode != ''">
                dt.dict_code = #{pageQuery.dictCode}
            </if>
            <if test="pageQuery.isEnabled != null">
                AND dt.is_enabled = #{pageQuery.isEnabled}
            </if>
            AND dt.is_deleted = false
        </where>
    </select>

</mapper>