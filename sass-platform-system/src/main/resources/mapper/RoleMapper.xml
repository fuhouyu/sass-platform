<?xml version="1.0" encoding="UTF-8"?>
<!--
  Copyright 2024-2025 fuhouyu.
 
  Licensed under the Apache License, Version 2.0 (the "License");
  you may not use this file except in compliance with the License.
  You may obtain a copy of the License at
 
       https://www.apache.org/licenses/LICENSE-2.0
 
  Unless required by applicable law or agreed to in writing, software
  distributed under the License is distributed on an "AS IS" BASIS,
  WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
  See the License for the specific language governing roles and
  limitations under the License.
-->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fuhouyu.sass.platform.system.mapper.RoleMapper">
    <resultMap id="BaseResultMap" type="com.fuhouyu.sass.platform.system.domain.entity.Roles" extends="com.fuhouyu.sass.platform.system.mapper.BaseMapper.BaseResultMap">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="role_name" property="roleName" jdbcType="VARCHAR"/>
        <result column="role_code" property="roleCode" jdbcType="VARCHAR"/>
        <result column="display_order" property="displayOrder" jdbcType="INTEGER"/>
        <result column="data_scope" property="dataScope" jdbcType="VARCHAR"/>
        <result column="is_enabled" property="isEnabled" jdbcType="BOOLEAN"/>
        <result column="is_allow_modified" property="isAllowModified" jdbcType="BOOLEAN"/>
        <result column="owner_tenant_id" property="ownerTenantId" jdbcType="VARCHAR"/>
    </resultMap>

    <sql id="TableName">
        roles
    </sql>

    <sql id="AllColumns" >
        id,
       role_name,
       role_code,
       display_order,
       data_scope,
       owner_tenant_id,
       is_enabled,
       is_allow_modified,
       is_deleted,
       created_at,
       updated_at,
       created_by,
       updated_by
    </sql>

    <sql id="QueryColumns" >
        r.id,
       r.role_name,
       r.role_code,
       r.display_order,
       r.data_scope,
       r.owner_tenant_id,
       r.is_enabled,
       r.is_allow_modified,
       r.is_deleted,
       r.created_at,
       r.updated_at,
       r.created_by,
       r.updated_by
    </sql>

    <insert id="insert" parameterType="com.fuhouyu.sass.platform.system.domain.entity.Roles">
        INSERT INTO
        <include refid="TableName"/>
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <include refid="AllColumns"/>
        </trim>
        VALUES
        <trim prefix="(" suffix=")" suffixOverrides=",">
            #{id},
            #{roleName},
            #{roleCode},
            #{displayOrder},
            #{dataScope},
            #{ownerTenantId},
            #{isEnabled},
            #{isAllowModified},
            #{isDeleted},
            #{createdAt},
            #{updatedAt},
            #{createdBy},
            #{updatedBy},
        </trim>
    </insert>

    <insert id="insertBatch" parameterType="java.util.List" >
        INSERT INTO
        <include refid="TableName"/>
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <include refid="AllColumns"/>
        </trim>
        VALUES
        <foreach collection="list" separator="," item="entity">
            <trim prefix="(" suffix=")" suffixOverrides=",">
                #{entity.id},
                #{entity.roleName},
                #{entity.roleCode},
                #{entity.displayOrder},
                #{entity.dataScope},
                #{entity.ownerTenantId},
                #{entity.isEnabled},
                #{entity.isAllowModified},
                #{entity.isDeleted},
                #{entity.createdAt},
                #{entity.updatedAt},
                #{entity.createdBy},
                #{entity.updatedBy},
            </trim>
        </foreach>
    </insert>

    <update id="update" parameterType="com.fuhouyu.sass.platform.system.domain.entity.Roles">
        UPDATE
        <include refid="TableName"/>
        SET

        <if test="roleName != null and roleName != ''">
            role_name = #{roleName},
        </if>
        <if test="displayOrder != null ">
            display_order = #{displayOrder},
        </if>
        <if test="isEnabled != null ">
            is_enabled = #{isEnabled},
        </if>
        <if test="dataScope != null and dataScope != ''">
            data_scope = #{dataScope},
        </if>
        updated_at = #{updatedAt},
        updated_by = #{updatedBy}
        WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="java.lang.Long">
        DELETE FROM <include refid="TableName"/>
        WHERE id = #{id}
    </delete>

    <delete id="deleteByIds" parameterType="java.util.List">
        DELETE FROM <include refid="TableName"/>
        WHERE id IN
        <foreach collection="list" open="(" close=")" separator="," item="id">
            #{id}
        </foreach>
    </delete>

    <select id="queryById" resultMap="BaseResultMap">
        SELECT <include refid="QueryColumns"/>
        FROM <include refid="TableName"/> r
        WHERE r.id = #{id}
    </select>


    <select id="queryByIds" parameterType="java.util.List" resultMap="BaseResultMap">
        SELECT <include refid="QueryColumns"/>
        FROM <include refid="TableName"/> r
        WHERE r.id IN
        <foreach collection="list" open="(" close=")" separator="," item="id">
            #{id}
        </foreach>
    </select>


    <select id="queryByRoleCode" parameterType="java.lang.String" resultMap="BaseResultMap">
        SELECT <include refid="QueryColumns"/>
        FROM <include refid="TableName"/> r
        WHERE r.role_code = #{roleCode}
    </select>

    <select id="queryList" resultMap="BaseResultMap" >
        SELECT
        <include refid="QueryColumns"/>
                FROM
        <include refid="TableName"/> r
        <where>
            <if test="pageQuery.roleCode != null and pageQuery.roleCode != ''">
                r.role_code = #{pageQuery.roleCode}
            </if>
            <if test="pageQuery.isEnabled != null">
                AND r.is_enabled = #{pageQuery.isEnabled}
            </if>
            AND r.is_deleted = false
        </where>
    </select>

    <delete id="deleteByTenantIds">
        DELETE FROM
        <include refid="TableName"/>
        WHERE owner_tenant_id IN
        <foreach collection="tenantIds" open="(" close=")" separator="," item="tenantId" >
            #{tenantId}
        </foreach>
    </delete>

    <select id="queryByRoleCodeByTenantId" resultMap="BaseResultMap">
        SELECT <include refid="QueryColumns"/>
        FROM <include refid="TableName"/> r
        WHERE r.role_code = #{roleCode} AND r.owner_tenant_id = #{tenantId}
    </select>
</mapper>